import React from 'react';
import { Box, PseudoBox, Button } from '@chakra-ui/core';
import img from '../img/new-beer.svg';

// eslint-disable-next-line import/prefer-default-export
export const NewBeer = () => {
  return (
    <Box />
  );
};
